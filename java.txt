import java.util.ArrayList;
import java.util.*;
import java.lang.*;
class seat
{
    public String sno;
    public boolean booked;

    public seat(String sno, boolean booked) {
        this.sno = sno;
        this.booked = booked;
    }

    public String getSno() {
        return sno;
    }

    public void setSno(String sno) {
        this.sno = sno;
    }

    public boolean isBooked() {
        return booked;
    }

    public void setBooked(boolean booked) {
        this.booked = booked;
    }


}

public class seatArrangement {
    public static void main(String[] args)
    {
        int r=5,c=10;
        Map<String,List<seat>> map=new TreeMap<>();
        for(int i=0;i<r;i++)
        {
            List<seat> l1=new ArrayList<>();
            for(int j=0;j<c;j++)
            {
                String st= Integer.toString(i)+Integer.toString(j);
                seat s=new seat(st,false);
                l1.add(s);
            }

            map.put(Integer.toString(i),l1);
        }

        String seatno=Integer.toString(1)+Integer.toString(2);
        Set set=map.entrySet();
        Iterator itr= set.iterator();
        while (itr.hasNext())
        {
            Map.Entry E=(Map.Entry)itr.next();
            if(E.getKey()==seatno.charAt(0))
        }